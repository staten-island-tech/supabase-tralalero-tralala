export type AppUser = {
    id: string;
    username: string;
    email: string;
  }
  
  export type LoginRequest = {
    email: string;
    password: string;
  }
  
  export type LoginResponse = {
  }